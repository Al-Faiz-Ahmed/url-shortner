// import * as z from "zod";
// import { ICreateUser } from "../../types/models";

// export const vCreateUser = z.object({
//   //   email: z.email(),
//   //   firstName: z.string().nullable(),
//   //   lastName:z.string().nullable(),
//   //   password:z.string().min(8,'password must be 8 charachters'),
//   //   username:z.string()
//   ipAddress: z.string(),
//   totalShortenedURL:z.number({error:"Total URL Shortned should be a number"}),
// }) satisfies z.ZodType<ICreateUser>;
